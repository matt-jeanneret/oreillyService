package com.oreilly.demo;

public enum TenderType {
    //There may be more of these. This is an enum because there are only so many tender types and that way it has to conform to a standard. 
    CASH, CREDIT
}
